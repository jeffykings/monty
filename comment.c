#include "monty.h"

/**
 * comment- doesnt compile a line once it sees #
 * @stack: pointer to the head
 * @line_number: the line to be printed
 */

void comment(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
